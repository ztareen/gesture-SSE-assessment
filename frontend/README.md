This folder contains a minimal React app (Vite + React) intended as a small browser demo to visualize scores and explanations.

To run locally:

```bash
cd frontend
npm install
npm run dev
```

Build for production with `npm run build` and serve the generated `dist/` folder.
